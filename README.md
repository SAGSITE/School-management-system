# X'TRA 'O' CHOICE ACADEMY School Management System

Open `index.html` in a browser to use the responsive demo.

Demo logins:

- Admin: `admin` / `admin123`
- Teacher: `teacher` / `teacher123`
- Student: `student` / `student123`
- Parent: `parent` / `parent123`

The app saves changes in the browser on this device using local storage.
